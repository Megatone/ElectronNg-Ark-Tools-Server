{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "Electron Angular Ark Tools Server",
  "main": "main.js",
  "scripts": {
    "test": "cd ng-app && ng build --output-path dist --base-href . && cd ..",
    "start": "cd ng-app && ng build --output-path dist --base-href . && cd ..  && electron .",
    "build": "npm test && electron-packager ./ ElectronArkToolsServer --platform=win32 --arch=x64 --overwrite"
  },
  "author": "Megatone",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^3.0.10",
    "electron-packager": "^12.2.0"
  }
}